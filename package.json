{
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.7.0",
    "fs-extra": "^10.0.0",
    "truffle-assertions": "^0.9.2"
  },
  "scripts": {
    "test": "truffle test --network gananche",
    "build": "truffle migrate --reset && ./contractMin"
  }
}
